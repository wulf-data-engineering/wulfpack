version: v1
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX   # Allows "package foo" instead of "package foo.v1"
    - PACKAGE_DIRECTORY_MATCH  # Allows "foo.proto" at root to contain "package foo"
breaking:
  use:
    - FILE
