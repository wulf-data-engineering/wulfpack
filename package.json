{
  "name": "wulfpack-toolset",
  "version": "1.0.0",
  "description": "Tool-set for Wulfpack monorepo",
  "main": "index.js",
  "scripts": {
    "new": "cookiecutter template",
    "test": "echo \"No tool-set tests yet.\"",
    "lint": "npm run lint:frontend",
    "lint:frontend": "echo 'Linting frontend template files...' && rm -rf template-lint-tmp && cp -r template template-lint-tmp && cd template-lint-tmp/{[cookiecutter.project_slug]}/frontend && npm install && npm run lint && cd ../../.. && rm -rf template-lint-tmp && echo 'Frontend template linting complete.'",
    "format": "npm run format:frontend",
    "format:frontend": "npx prettier --write 'template/{[cookiecutter.project_slug]}/frontend/**/*.{js,ts,json,css,md,html}'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cookiecutter": "^1.3.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0"
  }
}
